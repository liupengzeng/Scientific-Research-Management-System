<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.university.research.system.mapper.SysRoleMapper">

    <resultMap id="RoleResult" type="com.university.research.system.domain.SysRole">
        <id property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="selectRoleList" parameterType="com.university.research.system.domain.SysRole" resultMap="RoleResult">
        SELECT
            role_id, role_name, role_key, role_sort, status,
            create_by, create_time, update_by, update_time, remark
        FROM sys_role
        <where>
            <if test="roleName != null and roleName != ''">
                AND role_name LIKE CONCAT('%', #{roleName}, '%')
            </if>
            <if test="roleKey != null and roleKey != ''">
                AND role_key LIKE CONCAT('%', #{roleKey}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY role_sort ASC, role_id ASC
    </select>

    <select id="selectRoleById" parameterType="long" resultMap="RoleResult">
        SELECT
            role_id, role_name, role_key, role_sort, status,
            create_by, create_time, update_by, update_time, remark
        FROM sys_role
        WHERE role_id = #{roleId}
    </select>

    <insert id="insertRole" parameterType="com.university.research.system.domain.SysRole" useGeneratedKeys="true" keyProperty="roleId">
        INSERT INTO sys_role (role_name, role_key, role_sort, status, create_by, create_time, update_by, update_time, remark)
        VALUES (#{roleName}, #{roleKey}, #{roleSort}, #{status}, #{createBy}, NOW(), #{updateBy}, NOW(), #{remark})
    </insert>

    <update id="updateRole" parameterType="com.university.research.system.domain.SysRole">
        UPDATE sys_role
        SET role_name = #{roleName},
            role_key  = #{roleKey},
            role_sort = #{roleSort},
            status    = #{status},
            update_by = #{updateBy},
            update_time = NOW(),
            remark    = #{remark}
        WHERE role_id = #{roleId}
    </update>

    <delete id="deleteRoleById" parameterType="long">
        DELETE FROM sys_role WHERE role_id = #{roleId}
    </delete>

    <update id="updateStatus">
        UPDATE sys_role SET status = #{status}, update_time = NOW()
        WHERE role_id = #{roleId}
    </update>

    <select id="countUserRole" parameterType="long" resultType="int">
        SELECT COUNT(1) FROM sys_user_role WHERE role_id = #{roleId}
    </select>

    <select id="checkRoleNameUnique" resultType="int">
        SELECT COUNT(1)
        FROM sys_role
        WHERE role_name = #{roleName}
        <if test="roleId != null">
            AND role_id != #{roleId}
        </if>
    </select>

    <select id="checkRoleKeyUnique" resultType="int">
        SELECT COUNT(1)
        FROM sys_role
        WHERE role_key = #{roleKey}
        <if test="roleId != null">
            AND role_id != #{roleId}
        </if>
    </select>


    <!-- 结果映射 -->
 <!--   <resultMap id="RoleResult" type="com.university.research.system.domain.SysRole">
        <id property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>
-->
    <!-- 查询角色列表（仅查询正常状态的角色） -->
<!--
    <select id="selectRoleList" resultMap="RoleResult">
        SELECT
            role_id,
            role_name,
            role_key,
            role_sort,
            status,
            create_by,
            create_time,
            update_by,
            update_time,
            remark
        FROM sys_role
        WHERE status = '0'
        ORDER BY role_sort ASC, role_id ASC
    </select>
-->

</mapper>

