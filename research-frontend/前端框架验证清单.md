# 前端框架验证清单

**验证时间**: 2025-01-20  
**项目**: 高校科研管理系统前端

---

## 验证步骤

### 1. 依赖安装验证

#### ✅ 检查项
- [x] package.json 存在且配置正确
- [x] 所有依赖已安装（通过 `npm list` 验证）

#### 执行命令
```bash
cd research-frontend
npm install
```

**当前状态**: 依赖已安装，版本如下：
- Vue: 3.5.22
- Vue Router: 4.6.3
- Pinia: 2.3.1
- Element Plus: 2.11.7
- Axios: 1.13.1
- NProgress: 0.2.0
- js-cookie: 3.0.5

---

### 2. 开发服务器启动验证

#### 检查项
- [ ] 开发服务器可以正常启动
- [ ] 无编译错误
- [ ] 无运行时错误
- [ ] 控制台无警告（除ESLint警告外）

#### 执行命令
```bash
cd research-frontend
npm run dev
```

**预期结果**:
- 开发服务器启动在 http://localhost:3000
- 浏览器可以正常访问
- 无编译错误

---

### 3. 配置文件验证

#### 3.1 Vite配置验证
- [x] `vite.config.js` 配置正确
  - ✅ 路径别名 `@` 配置正确
  - ✅ API代理配置正确（/api -> http://localhost:8080）
  - ✅ Element Plus自动导入配置正确
  - ✅ 开发服务器端口：3000

#### 3.2 ESLint配置验证
- [x] `eslint.config.js` 使用JavaScript配置
- [x] 无TypeScript相关配置

#### 3.3 环境变量验证
- [ ] 创建 `.env` 文件（如果需要）
- [ ] 环境变量配置正确

---

### 4. 核心功能验证

#### 4.1 HTTP请求封装验证
**文件**: `src/utils/request.js`
- [x] Axios实例创建正确
- [x] 请求拦截器配置正确（自动添加Token）
- [x] 响应拦截器配置正确（统一错误处理）
- [ ] 可以正常发送请求（需要后端运行）

**测试方法**:
```javascript
// 在浏览器控制台测试
import request from '@/utils/request'
request.get('/auth/userInfo').then(res => console.log(res))
```

#### 4.2 认证工具验证
**文件**: `src/utils/auth.js`
- [x] Token存储/获取/删除功能完整
- [x] 用户信息存储/获取功能完整

**测试方法**:
```javascript
// 在浏览器控制台测试
import { setToken, getToken, removeToken } from '@/utils/auth'
setToken('test-token')
console.log(getToken()) // 应输出: test-token
removeToken()
console.log(getToken()) // 应输出: null
```

#### 4.3 Pinia状态管理验证
**文件**: `src/stores/user.js`, `src/stores/permission.js`, `src/stores/app.js`
- [x] Store定义正确
- [x] 状态、getters、actions配置正确
- [ ] 可以正常使用（需要测试）

**测试方法**:
```javascript
// 在浏览器控制台测试
import { useUserStore } from '@/stores/user'
const userStore = useUserStore()
console.log(userStore.isLoggedIn)
```

#### 4.4 路由配置验证
**文件**: `src/router/index.js`
- [x] 路由定义正确
- [x] 路由守卫配置正确
- [ ] 路由跳转正常

**测试路径**:
- `/login` - 登录页
- `/dashboard` - 首页（需要登录）
- `/404` - 404错误页
- `/401` - 401错误页

#### 4.5 布局组件验证
**文件**: `src/layout/`
- [x] 主布局组件存在
- [x] 头部组件存在
- [x] 侧边栏组件存在
- [x] 内容区组件存在
- [ ] 页面可以正常渲染

---

### 5. 页面组件验证

#### 5.1 登录页面
**文件**: `src/views/login/index.vue`
- [x] 登录表单存在
- [x] 表单验证配置正确
- [ ] 可以正常显示（需要浏览器验证）

#### 5.2 首页
**文件**: `src/views/dashboard/index.vue`
- [x] 首页组件存在
- [x] 统计卡片配置正确
- [ ] 可以正常显示

#### 5.3 错误页面
**文件**: `src/views/error/404.vue`, `src/views/error/401.vue`
- [x] 404页面存在
- [x] 401页面存在
- [ ] 可以正常显示

---

### 6. 公共组件验证

#### 6.1 分页组件
**文件**: `src/components/Pagination/index.vue`
- [x] 组件存在
- [x] 支持分页和每页大小调整
- [ ] 可以正常使用（需要测试）

#### 6.2 文件上传组件
**文件**: `src/components/Upload/index.vue`
- [x] 组件存在
- [x] 支持文件类型和大小验证
- [ ] 可以正常使用（需要测试）

#### 6.3 图片预览组件
**文件**: `src/components/ImagePreview/index.vue`
- [x] 组件存在
- [ ] 可以正常使用（需要测试）

---

### 7. 样式验证

#### 检查项
- [x] 全局样式文件存在（`src/styles/index.scss`）
- [x] NProgress样式引入正确
- [x] Element Plus样式引入正确
- [ ] 页面样式正常显示

---

### 8. 浏览器兼容性验证

#### 检查项
- [ ] Chrome浏览器测试通过
- [ ] Edge浏览器测试通过
- [ ] Firefox浏览器测试通过（可选）
- [ ] Safari浏览器测试通过（可选）

---

## 验证命令

### 启动开发服务器
```bash
cd research-frontend
npm run dev
```

### 运行ESLint检查
```bash
npm run lint
```

### 构建生产版本
```bash
npm run build
```

---

## 已知问题

### 待后端接口完成后验证
1. **登录功能**: 需要后端 `/api/auth/login` 接口
2. **获取用户信息**: 需要后端 `/api/auth/userInfo` 接口
3. **获取菜单列表**: 需要后端 `/api/system/menu/list` 接口
4. **动态路由**: 需要菜单数据后才能正常工作

### 可能需要调整的配置
1. **环境变量**: 如果API地址不是 `http://localhost:8080`，需要创建 `.env` 文件配置
2. **CORS配置**: 如果后端CORS配置不同，可能需要调整

---

## 验证结果记录

### 基础配置验证
- [x] 依赖安装完成
- [ ] 开发服务器启动成功
- [ ] 无编译错误
- [ ] 无运行时错误

### 功能验证
- [ ] HTTP请求封装正常
- [ ] 路由跳转正常
- [ ] 状态管理正常
- [ ] 布局组件正常显示
- [ ] 登录页面正常显示

### 集成验证（需要后端）
- [ ] 登录接口调用成功
- [ ] Token存储和获取正常
- [ ] 路由守卫正常工作
- [ ] 动态菜单加载正常

---

**下一步**: 执行验证步骤，记录验证结果

---

## 快速验证命令

```bash
# 进入前端目录
cd research-frontend

# 启动开发服务器（最重要）
npm run dev

# 运行ESLint检查
npm run lint
```

**注意**: 
- 开发服务器应在 http://localhost:3000 启动
- 访问登录页面测试基础功能
- 后端接口完成后进行完整集成测试

